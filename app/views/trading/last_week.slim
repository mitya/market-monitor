- page_title! 'Week / Gainers'

- fields = %i[icon ticker change rel_volume volume]
main.container-fluid
  .row
    - @results.each do |date, result|
      .col-2
        h6.text-end= l date, format: :rus_with_week_day
        = data_table fields: fields, class: "table table-sm table-hover table-font-sm tickers-table table-selectable momentum-table" do
          = render 'instruments_eod', candles: result.gainers
          = render 'instruments_eod', candles: result.losers, header: false
          = render 'instruments_eod', candles: result.volume_gainers, header: false
